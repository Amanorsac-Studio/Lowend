# Build output — regenerated by CMake, never committed.
build/
build-mac/
build-tester/
build-mac-tester/
release/

# The separation model — fetched at build/package time, checked by SHA-256
# against the sizes recorded in scripts/build-*; never committed (171 MB).
models/

# Editor/OS cruft
.vs/
.vscode/
*.user
.DS_Store
Thumbs.db
